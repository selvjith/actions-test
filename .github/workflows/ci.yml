name: test
on:
  push:
    branches: [ main ]
jobs:
  concatinate:
    runs-on: ubuntu-latest
    steps:
    - id: checkout
      uses: actions/checkout@v2
      with:
        ref: main
    - run: cat ta/internal/*.pem > single.pem
