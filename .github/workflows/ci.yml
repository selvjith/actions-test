name: test
on:
  workflow_dispatch:
    inputs:
      my_value:
        description: 'My input value'
        required: true
        default: 'foo'
        type: string
jobs:
  concatinate:
    runs-on: ubuntu-latest
    steps:
    - id: checkout
      uses: actions/checkout@v2
      with:
        ref: main
    - run: |-
       cat ta/internal/*.pem > single.pem
       wget google.com
